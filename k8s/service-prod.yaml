apiVersion: v1
kind: Service
metadata:
  name: study-app
  namespace: prod-study
spec:
  selector:
    app: study-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort