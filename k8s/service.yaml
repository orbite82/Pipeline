apiVersion: v1
kind: Service
metadata:
  name: study-app   # <-- troque aqui
  namespace: dev-study
spec:
  selector:
    app: study-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort